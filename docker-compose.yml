services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: solarops
      POSTGRES_PASSWORD: solarops
      POSTGRES_DB: solarops
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
